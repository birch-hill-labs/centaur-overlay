[project]
name = "centaur-acme-overlay"
version = "0.1.0"
description = "Forkable ACME example overlay for Centaur"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
